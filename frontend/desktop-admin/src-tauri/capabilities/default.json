{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — no Tauri commands, pure WebView",
  "windows": ["main"],
  "permissions": ["core:default"]
}
